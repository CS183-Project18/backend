<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.storefinds.uniquefindsbackend.mapper.CommentLikeMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO comment_likes (user_id, comment_id)
        VALUES (#{userId}, #{commentId})
    </insert>

    <delete id="deleteByUserIdAndCommentId">
        DELETE FROM comment_likes
        WHERE user_id = #{userId}
          AND comment_id = #{commentId}
    </delete>

    <select id="countByUserIdAndCommentId" resultType="int">
        SELECT COUNT(1)
        FROM comment_likes
        WHERE user_id = #{userId}
          AND comment_id = #{commentId}
    </select>

    <select id="selectLikedCommentIds" resultType="long">
        SELECT comment_id
        FROM comment_likes
        WHERE user_id = #{userId}
          AND comment_id IN
        <foreach collection="commentIds" item="commentId" open="(" separator="," close=")">
            #{commentId}
        </foreach>
    </select>

</mapper>
