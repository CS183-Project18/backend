<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.storefinds.uniquefindsbackend.mapper.InteractionEventMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO interaction_events (
            event_type,
            user_id,
            post_id,
            comment_id,
            target_type,
            target_id,
            event_value,
            metadata
        ) VALUES (
            #{eventType},
            #{userId},
            #{postId},
            #{commentId},
            #{targetType},
            #{targetId},
            #{eventValue},
            #{metadata}
        )
    </insert>

</mapper>
