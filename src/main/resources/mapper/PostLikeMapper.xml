<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.storefinds.uniquefindsbackend.mapper.PostLikeMapper">

    <insert id="insertIgnore">
        INSERT IGNORE INTO post_likes (user_id, post_id)
        VALUES (#{userId}, #{postId})
    </insert>

    <delete id="deleteByUserIdAndPostId">
        DELETE FROM post_likes
        WHERE user_id = #{userId}
          AND post_id = #{postId}
    </delete>

    <select id="countByUserIdAndPostId" resultType="int">
        SELECT COUNT(1)
        FROM post_likes
        WHERE user_id = #{userId}
          AND post_id = #{postId}
    </select>

    <select id="selectLikedPostIds" resultType="long">
        SELECT post_id
        FROM post_likes
        WHERE user_id = #{userId}
          AND post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </select>

</mapper>
